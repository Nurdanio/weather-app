import { createStyles } from '@mantine/core'

export const useNextForeCastStyles = createStyles(() => ({
  stack: {
    width: '335px',
    backgroundColor: 'rgb(0, 16, 38, 0.3)',
    borderRadius: 20,
    padding: 15,
  },
  flex: {
    width: '100%',
    justifyContent: 'space-between',
    color: '#FFFFFF',
    fontSize: 18,
    fontWeight: 700,
    alignItems: 'center',
  },
}))
