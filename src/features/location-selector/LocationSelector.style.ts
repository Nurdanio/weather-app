import { createStyles } from '@mantine/core'

export const useLocationSelectorStyle = createStyles({
  selector: {
    backgroundColor: 'inherit',
    input: {
      width: 175,
      border: '0',
      fontSize: 18,
      fontWeight: 600,
      color: '#FFFFFF',
      background: 'inherit',
    },
  },
})
